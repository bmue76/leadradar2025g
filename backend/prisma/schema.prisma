// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// Models folgen in späteren Teilprojekten (Forms, Leads, Users, Tenants, ...)
